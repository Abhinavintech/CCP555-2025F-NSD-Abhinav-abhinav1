# .env.example - Copy this file to .env and fill in your values

# Port to use when starting the server
PORT=8080

# Which log messages to show (usually `info` for production, `debug` for development, `silent` to disable)
LOG_LEVEL=debug

# AWS Amazon Cognito User Pool ID (use your User Pool ID)
# AWS_COGNITO_POOL_ID=us-east-1_pBCBqXzGk

# AWS Amazon Cognito Client App ID (use your Client App ID)
# AWS_COGNITO_CLIENT_ID=47liqj42ob1upblrqed7stfsr8

# Secret for hashing user emails for privacy protection
HASH_SECRET=your-super-secret-hash-key-change-this-in-production

# HTTP Basic Auth (for testing - create tests/.htpasswd file)
# HTPASSWD_FILE=tests/.htpasswd